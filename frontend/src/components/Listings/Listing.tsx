
interface ListingProps {
    tags: Array<string>,
    description: string,
    price: number,
    fav: boolean
}

export default function Listing({tags, description, price, fav}: ListingProps){
    return <></>;
}